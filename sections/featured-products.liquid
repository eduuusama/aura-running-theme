{%- comment -%}
  Featured Products — Centered header + 3-column product row
  Cards mirror the Shop page product-card (left-aligned, dual-image
  hover, full-width uppercase button). Sold out renders grey.
{%- endcomment -%}

<section class="py-20 md:py-28 bg-background">
  <div class="container">
    {%- comment -%} Centered header (hidden entirely when empty) {%- endcomment -%}
    {%- if section.settings.label != blank or section.settings.heading != blank or section.settings.description != blank -%}
      <div class="text-center md:text-left max-w-2xl mx-auto md:mx-0 mb-16 md:mb-20">
        {%- if section.settings.label != blank -%}
          <p class="text-xs tracking-[0.2em] uppercase text-muted-foreground mb-5 font-light">
            {{ section.settings.label }}
          </p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          {%- if section.settings.heading_is_h1 -%}
            <h1 class="font-serif text-3xl md:text-4xl mb-5">
              {{ section.settings.heading }}
            </h1>
          {%- else -%}
            <h2 class="font-serif text-3xl md:text-4xl mb-5">
              {{ section.settings.heading }}
            </h2>
          {%- endif -%}
        {%- endif -%}
        {%- if section.settings.description != blank -%}
          <p class="text-base text-muted-foreground font-light leading-relaxed">
            {{ section.settings.description }}
          </p>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- comment -%} Product columns — swipeable carousel on mobile, grid from sm up {%- endcomment -%}
    <div class="flex overflow-x-auto snap-x snap-mandatory scrollbar-hide gap-4 -mx-8 px-8 pb-2 sm:grid sm:grid-cols-2 lg:grid-cols-3 sm:gap-8 sm:mx-0 sm:px-0 sm:pb-0 sm:overflow-visible">
      {%- for block in section.blocks -%}
        {%- assign fp = block.settings.product -%}
        {%- if fp != blank -%}
          <div class="group block bg-background w-[78vw] max-w-sm shrink-0 snap-center sm:w-auto sm:max-w-none sm:shrink" {{ block.shopify_attributes }}>
            <a href="{{ fp.url }}" class="block">
              <div class="aspect-square overflow-hidden bg-muted mb-6 relative">
                {%- if fp.featured_image -%}
                  <img
                    src="{{ fp.featured_image | image_url: width: 800 }}"
                    alt="{{ fp.featured_image.alt | default: fp.title }}"
                    class="product-card-image-main w-full h-full object-cover transition-opacity duration-500"
                    loading="lazy"
                  >
                {%- endif -%}
                {%- if fp.images.size > 1 -%}
                  <img
                    src="{{ fp.images[1] | image_url: width: 800 }}"
                    alt="{{ fp.images[1].alt | default: fp.title }}"
                    class="product-card-image-hover w-full h-full object-cover"
                    loading="lazy"
                  >
                {%- endif -%}
              </div>
            </a>

            <div class="px-1">
              <a href="{{ fp.url }}">
                <h3 class="text-base font-medium tracking-tight mb-1 group-hover:opacity-60 transition-opacity">
                  {{ fp.title }}
                </h3>
              </a>

              {%- if block.settings.descriptor != blank -%}
                <p class="text-sm text-muted-foreground font-light line-clamp-2 mb-3">
                  {{ block.settings.descriptor }}
                </p>
              {%- endif -%}

              {%- if block.settings.bullet_1 != blank or block.settings.bullet_2 != blank or block.settings.bullet_3 != blank -%}
                <ul class="text-left mb-4 space-y-1">
                  {%- if block.settings.bullet_1 != blank -%}
                    <li class="text-sm text-muted-foreground font-light flex items-start gap-2">
                      <span class="mt-1.5 w-1 h-1 rounded-full bg-muted-foreground shrink-0"></span>
                      {{ block.settings.bullet_1 }}
                    </li>
                  {%- endif -%}
                  {%- if block.settings.bullet_2 != blank -%}
                    <li class="text-sm text-muted-foreground font-light flex items-start gap-2">
                      <span class="mt-1.5 w-1 h-1 rounded-full bg-muted-foreground shrink-0"></span>
                      {{ block.settings.bullet_2 }}
                    </li>
                  {%- endif -%}
                  {%- if block.settings.bullet_3 != blank -%}
                    <li class="text-sm text-muted-foreground font-light flex items-start gap-2">
                      <span class="mt-1.5 w-1 h-1 rounded-full bg-muted-foreground shrink-0"></span>
                      {{ block.settings.bullet_3 }}
                    </li>
                  {%- endif -%}
                </ul>
              {%- endif -%}

              <p class="text-sm font-light mb-4">{{ fp.price | money }}</p>

              {%- if fp.available -%}
                <button
                  class="w-full h-14 rounded-none bg-foreground text-background hover:bg-foreground/90 transition-colors text-xs font-medium uppercase tracking-[0.15em]"
                  onclick="event.preventDefault(); document.dispatchEvent(new CustomEvent('cart:add', { detail: { id: {{ fp.selected_or_first_available_variant.id }} } }))"
                >
                  {{ 'general.add_to_cart' | t | default: 'Add to Cart' }}
                </button>
              {%- else -%}
                <button
                  class="w-full h-14 rounded-none bg-muted text-muted-foreground hover:bg-muted/80 transition-colors text-xs font-medium uppercase tracking-[0.15em]"
                  onclick="event.preventDefault(); window.openWaitlist('{{ fp.handle }}', '{{ fp.title | escape }}')"
                >
                  Sold Out &ndash; Join Waitlist
                </button>
              {%- endif -%}
            </div>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Featured Products",
  "settings": [
    { "type": "text", "id": "label", "label": "Label" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "The System" },
    { "type": "checkbox", "id": "heading_is_h1", "label": "Render heading as H1 (only one H1 per page)", "default": false },
    { "type": "textarea", "id": "description", "label": "Description" }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Product",
      "settings": [
        { "type": "product", "id": "product", "label": "Product" },
        { "type": "textarea", "id": "descriptor", "label": "Short Descriptor" },
        { "type": "text", "id": "bullet_1", "label": "Bullet 1" },
        { "type": "text", "id": "bullet_2", "label": "Bullet 2" },
        { "type": "text", "id": "bullet_3", "label": "Bullet 3" }
      ]
    }
  ],
  "presets": [{ "name": "Featured Products" }]
}
{% endschema %}
